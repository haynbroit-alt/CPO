# World: evolutionary  — DEAP genetic algorithms + Mesa agent-based modelling
FROM python:3.11-slim

RUN pip install --no-cache-dir deap mesa

RUN groupadd -r sandbox && useradd -r -g sandbox sandbox
USER sandbox
WORKDIR /tmp

ENTRYPOINT ["python"]
