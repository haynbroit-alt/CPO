# World: bayesian  — NumPyro + PyMC probabilistic programming
FROM python:3.11-slim

RUN pip install --no-cache-dir numpyro pymc

RUN groupadd -r sandbox && useradd -r -g sandbox sandbox
USER sandbox
WORKDIR /tmp

ENTRYPOINT ["python"]
