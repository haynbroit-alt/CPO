# World: symbolic  — SymPy + Z3 theorem prover
FROM python:3.11-slim

RUN pip install --no-cache-dir sympy z3-solver

RUN groupadd -r sandbox && useradd -r -g sandbox sandbox
USER sandbox
WORKDIR /tmp

ENTRYPOINT ["python"]
